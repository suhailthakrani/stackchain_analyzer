# Example

Run the programmatic API against the bundled fixture:

```sh
dart run example/example.dart
```

Or analyze any Flutter app from the CLI:

```sh
dart run stackchain_analyzer analyze --path /path/to/flutter_app --offline
```
