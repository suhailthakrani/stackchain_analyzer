name: sample_app
description: Fixture Flutter app for StackChain Analyzer tests.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  cached_network_image: ^3.3.0
  pedantic: ^1.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
